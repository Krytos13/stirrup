apiVersion: apps/v1
kind: Deployment
metadata:
  name: stirrup-splash
  labels:
    app.kubernetes.io/name: stirrup-splash
    app.kubernetes.io/part-of: stirrup
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: stirrup-splash
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stirrup-splash
        app.kubernetes.io/part-of: stirrup
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        fsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: nginx
          image: ghcr.io/krytos13/stirrup:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: nginx-cache
              mountPath: /var/cache/nginx
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 20
      volumes:
        - name: tmp
          emptyDir: {}
        - name: nginx-cache
          emptyDir: {}
